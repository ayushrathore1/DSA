#include<iostream>
#include<stdio.h>
#include<stdlib.h>

using namespace std;

int main()
{
    // int a = 10;
    // int *p; // decalaration
    // p = &a;

    // cout << a << endl;
    // printf("using pointer %d", *p);

    // int A[5] = {2,4,6,8,10};
    // int *p;
    // p = A;
     // or we can write the p = &A[0];
     // but we can't write p = &A;

     
     
     
    //  int *p;
    //  p = (int *)malloc(5 * sizeof(int));

    // p = new int[5];// dynamic allocation
    //  p[0] = 41; p[1] = 12;
    //  p[2] = 56; p[3] = 78;
    //  p[4] = 16;
    //  for(int i = 0; i < 5; i++)
    //      cout << p[i] << endl;
     
    //     delete [] p;
    //     free(p);

    return 0;
}